# What is CNN (Convolution Neural Network)?
---

## Application
- Pattern recognition

## Implimentation
using a 3*3 pixel and the slide(convo) until all the end

It will give a array of number how closely our image matches our filter.

We can add more filter as well with different shape (Pattern)

Pooling: 
First layer: Process of combining all the array output of the filter

Second layer: A bit deeper, more abstract filter (Door, window) --> Output is still a array

n layer: able to all more filter, determine what will shwo or regnoize


